REM Future example for a compiler I've started to work on...
PRINT "Hello, World!\n";
INTEGER A;
$A = (15+27)/2;
PRINT $A;
STRING S;
S = "yay!\n";
PRINT S;
IF A == 5 THEN GOTO PROC1 ELSE GOTO PROC2;
PROC1:
	QUIT;
PROC2:
	PRINT "noo";
	A = A & 12 | 0xFF;
	PRINT $A;
	QUIT;

