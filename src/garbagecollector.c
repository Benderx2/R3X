#include <r3x_garbage_collector.h>
/**
 * Initialize Garbage Collector, and return a GC_VARIABLE_LIST* which can be used later.
**/
GC_VARIABLE_LIST* InitializeGC(r3x_cpu_t* CPU) {
	(void)CPU;
	return NULL;
}
